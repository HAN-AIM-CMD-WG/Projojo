ENVIRONMENT=preview

# Backend Environment Variables (Non-Secret)
BACKEND_PORT=7000
TYPEDB_NAME=projojo_db
TYPEDB_HOST_NAME=typedb
TYPEDB_PORT=1729
TYPEDB_STUDIO_PORT=1728
TYPEDB_SERVER_ADDR=${TYPEDB_HOST_NAME}:${TYPEDB_PORT}
TYPEDB_EDITION=Core
TYPEDB_USERNAME=admin
TYPEDB_DEFAULT_PASSWORD=password

# Uncomment to reset the database on every startup (WARNING: deletes all data!)
# RESET_DB=yes

# Frontend Environment Variables (Non-Secret)
# Some of these must start with 'VITE_' to be accessible in the frontend code
VITE_APP_NAME="Projojo - student assignment portal"
VITE_APP_VERSION=0.7.0
# if VITE_BACKEND_HOST has a value, services.js will use HTTPS protocol.
VITE_BACKEND_HOST=backend.projojo.dp.demopreview.nl   
VITE_BACKEND_PORT=443
# FRONTEND_PORT get mapped to port 80 in Dokploy/Treafik
FRONTEND_PORT=4173   
 # FRONTEND_URL is used for OAuth redirects
FRONTEND_URL=https://projojo.dp.demopreview.nl

# ============================================================================
# Email Configuration (SMTP)
# ============================================================================
# TLS is auto-negotiated - just set host, port, and credentials (if needed)

# --- SMTP2GO (production) ---
EMAIL_DEFAULT_SENDER=noreply@projojo.nl
EMAIL_SMTP_HOST=mail-eu.smtp2go.com
EMAIL_SMTP_PORT=2525
EMAIL_SMTP_USERNAME=testSmurf
# EMAIL_SMTP_PASSWORD=don't set this value here! See below.

# ============================================================================
# Secrets for preview development. These MUST be set in Dokploy UI,
# because _this_ file _is_ committed to Github
# ============================================================================

# TYPEDB_NEW_PASSWORD=don't set this value here!

# EMAIL_SMTP_PASSWORD=don't set this value here!

# OAuth Provider Credentials. For detailed setup instructions, see: auth/README.md
# GOOGLE_CLIENT_ID=don't set this value here!
# GOOGLE_CLIENT_SECRET=don't set this value here!
# GITHUB_CLIENT_ID=don't set this value here!
# GITHUB_CLIENT_SECRET=don't set this value here!
# MICROSOFT_CLIENT_ID=don't set this value here!
# MICROSOFT_CLIENT_SECRET=don't set this value here!

# Security & Session Management: secret key for OAuth session management during login flows
# SESSIONS_SECRET_KEY=don't set this value here!
# Secret key for JWT token signing and verification
# JWT_SECRET_KEY=don't set this value here!
