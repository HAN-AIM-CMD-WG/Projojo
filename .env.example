# ============================================================================
# Environment Configuration Template
# ============================================================================
# This file serves as the template for both:
#   - Local development: Copy to .env and fill in secrets
#   - Dokploy preview: Copy contents to Dokploy's Environment tab
#
# Usage:
#   Development: cp .env.example .env  (then edit .env with your secrets)
#   Dokploy:     Paste this file's contents into Dokploy's Environment tab
#                and fill in the actual secret values (remove comments)
# ============================================================================

ENVIRONMENT=development
# For Dokploy preview, change to: ENVIRONMENT=preview

# ============================================================================
# Port Allocation Strategy
# ============================================================================
# All host-exposed ports use the 10101+ range to avoid conflicts with:
# - macOS, Windows and Ubuntu services (5000, 7000, 3389, 1433, 1194 etc.)
# - Common dev tool defaults (3000, 5173, 8000, 8080 etc.)
# - System services (ports < 1024)
#
# Port assignments:
#   10101 - Frontend (Vite)
#   10102 - Backend (FastAPI)
#   10103 - TypeDB
#   10104 - TypeDB Studio
#   10105 - MailHog SMTP
#   10106 - MailHog Web UI

# ============================================================================
# Backend Environment Variables (Non-Secret)
# ============================================================================
BACKEND_PORT=10102
TYPEDB_NAME=projojo_db
TYPEDB_HOST_NAME=typedb
TYPEDB_PORT=10103
TYPEDB_STUDIO_PORT=10104
TYPEDB_SERVER_ADDR=${TYPEDB_HOST_NAME}:1729
TYPEDB_EDITION=Core
TYPEDB_USERNAME=admin
TYPEDB_DEFAULT_PASSWORD=password

# Uncomment to reset the database on every startup (WARNING: deletes all data!)
# RESET_DB=yes

# ============================================================================
# Frontend Environment Variables (Non-Secret)
# Some of these must start with 'VITE_' to be accessible in the frontend code
# ============================================================================
VITE_APP_NAME="Projojo - student assignment portal"
VITE_APP_VERSION=0.7.0

# Development: leave VITE_BACKEND_HOST empty (uses current hostname)
VITE_BACKEND_HOST=
VITE_BACKEND_PORT=${BACKEND_PORT}
FRONTEND_PORT=10101
FRONTEND_URL=http://localhost:10101

# For Dokploy preview, use these values instead:
# VITE_BACKEND_HOST=backend.preview.projojo.nl
# VITE_BACKEND_PORT=443
# FRONTEND_URL=https://preview.projojo.nl

# ============================================================================
# SMTP Email Configuration (Non-Secret)
# ============================================================================
# TLS is auto-negotiated - just set host, port, and credentials (if needed)
#
# IMPORTANT: MailHog hostname depends on where you're running:
#   - From Docker (backend container): use "mailhog" (Docker network)
#   - From local machine (test script): use "localhost" (port forwarding)

# MailHog
EMAIL_DEFAULT_SENDER=noreply@projojo.nl
EMAIL_SMTP_HOST=mailhog
EMAIL_SMTP_PORT=10105
MAILHOG_WEB_PORT=10106
# username left empty for Mailhog
EMAIL_SMTP_USERNAME=

# --- SMTP2GO ---
# EMAIL_DEFAULT_SENDER=test-noreply@projojo.nl
# EMAIL_SMTP_HOST=mail-eu.smtp2go.com
# EMAIL_SMTP_PORT=2525
# EMAIL_SMTP_USERNAME=testSmurf
# EMAIL_SMTP_PASSWORD=don't set this value here! See below.



# ============================================================================
# Secrets, NEVER copy to production or preview env-files!
# ============================================================================

TYPEDB_NEW_PASSWORD=password

# password for Mailhog should remain empty
EMAIL_SMTP_PASSWORD=

# OAuth Provider Credentials. For detailed setup instructions, see: auth/README.md
# GOOGLE_CLIENT_ID=your_google_client_id_here
# GOOGLE_CLIENT_SECRET=your_google_client_secret_here
# GITHUB_CLIENT_ID=your_github_client_id_here
# GITHUB_CLIENT_SECRET=your_github_client_secret_here
# MICROSOFT_CLIENT_ID=your_microsoft_client_id_here
# MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret_here

# Security & Session Management: secret key for OAuth session management during login flows
# SESSIONS_SECRET_KEY=your_sessions_secret_key_here
# Secret key for JWT token signing and verification
# JWT_SECRET_KEY=your_jwt_secret_key_here

